{{#power-select
  options=@options
  selected=@selected
  beforeOptionsComponent=null
  triggerComponent="trigger-with-search"
  onchange=(action "onChange") as |opt|}}
  {{yield opt}}
{{/power-select}}